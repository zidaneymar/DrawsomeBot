<Page
    x:Class="NoteTaker.NoteTaker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NoteTaker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer ZoomMode="Enabled" MinZoomFactor="1" MaxZoomFactor="20" HorizontalScrollBarVisibility="Visible" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
              VerticalScrollBarVisibility="Visible" BorderBrush="LightBlue" BorderThickness="2">
            <Border Grid.Row ="0" BorderBrush="Black" BorderThickness="1">
                <InkCanvas x:Name="inkCanvas" HorizontalAlignment="Stretch" />
            </Border>
        </ScrollViewer>
        <Border>
            <InkToolbar x:Name="inkToolbar" VerticalAlignment="Bottom" TargetInkCanvas="{x:Bind inkCanvas}"></InkToolbar>
        </Border>
        <Border>
            <Button Content="Get Storkes And Save" Click="SubscribeButton_Click" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="32"/>
        </Border>
    </Grid>
</Page>
